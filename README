kinx
====

try to build a MIS to help the kinx company manage their business
the git diff and git diff  --staged are really different.
git diff HEAD can show the difference between the unstaged and you last commit 
test the command of git commit --amend

